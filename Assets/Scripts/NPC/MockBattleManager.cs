using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Class intended to mock a battle manager for testing NPC behavior. Depending on the key pressed, it makes the NPC subtract and add a set value
public class MockBattleManager : MonoBehaviour
{

    // Start is called before the first frame update

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
